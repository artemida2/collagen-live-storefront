node_modules/
node_modules/.tmp/
dist/
*.local
.DS_Store
Thumbs.db

# .impeccable holds a scratch shot set and a hook cache; the design record
# itself belongs in the repo.
.impeccable/*
!.impeccable/design.json
!.impeccable/config.json

# вторая сборка (страницы на Astro) и её кэш
pages/dist/
pages/.astro/
